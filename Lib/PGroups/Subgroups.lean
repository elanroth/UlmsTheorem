import Lib.PGroups.Defs

/-!
# Subgroup-level algebra for reduced abelian p-groups

This module is the new home for basic subgroup constructions such as `pPow`
and `pImage`.  The current implementation is re-exported from
`Lib.PGroups.Defs` while the library is being reorganized.
-/

